apiVersion: v1
kind: ConfigMap
metadata:
  name: epg-v3-migrator-{{ .Values.partner }}-conf
  namespace: default
data:
  appsettings.json: |
    {
      "PartnerId": {{ .Values.partner }},
      "BatchSize": 20000,
      "OriginalEpgVersion": 1
    }